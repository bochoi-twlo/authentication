{
  "name": "authentication-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.61.0",
    "jsonwebtoken": "^8.5.1"
  },
  "engines": {
    "node": "12"
  }
}
